# router

Reserved for `vue-router` setup if the app grows beyond a single view.

Not wired up — this task is one page, so introducing routing would be
solving a problem that doesn't exist yet. The folder is here so that
adding a second page later (e.g. a "suppliers" view) doesn't require
restructuring `main.js` and `App.vue` from scratch.
