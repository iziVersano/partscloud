# shared/components

Reserved for components reused across more than one feature (e.g. a
generic Modal, Pagination, or EmptyState).

Empty for now — this task only has one feature
(`features/inventory/`), so there's nothing to share yet. The folder
exists so that when a second feature needs a component the inventory
feature already has, it moves here instead of being duplicated or
creating a circular dependency between features.
